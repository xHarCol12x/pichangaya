export interface Venue {
    id: string;
    name: string;
    address: string;
    description?: string;
    tenantId: string;
    createdAt: string;
    updatedAt: string;
    deletedAt?: string;
}
